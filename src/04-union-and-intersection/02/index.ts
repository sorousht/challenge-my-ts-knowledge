import { customers, employees } from './book';
import { Customer, Employee } from './types';
/*
  FIXME

  You've got a book to enroll some people. The book has two sections
  for customers and employees. The below method should be smart enough
  to note the difference between and employee and a customer,
  then enroll the given person in the right section.
*/
export const enroll = (person: Employee | Customer) => {
  throw Error('Not implemented yet');
}