describe('types and comparison', () => {
  it('types are different', () => {
    expect(typeof undefined).toBe(/* FIXME */);
    expect(typeof null).toBe(/* FIXME */);
  });

  test('there is a way to check the equality', () => {
    expect(undefined === null).toBe(/* FIXME */);
    expect(undefined == null).toBe(/* FIXME */);
  });
});